<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_partner_form_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.sales_purchases</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales_purchases']" position="inside">
                <group string="Edi Configuration">
                    <!-- #TODO: Domain for purchase order
                    Context to create / edit directly: default_model=purchase.order, default_res_id=active_id -->
                    <field name="edi_purchase_conf_ids" />
                </group>
            </xpath>
        </field>
    </record>
</odoo>
