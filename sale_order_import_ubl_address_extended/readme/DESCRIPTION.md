This module implements correct parsing of partners' addresses from UBL files when
 importing sale orders. It extends the default behavior to handle street name and
 number according to module ``base_address_extended``.
